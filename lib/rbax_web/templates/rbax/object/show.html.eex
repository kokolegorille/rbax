<h1><%= @object.name %></h1>

<%= if length(@object.domains) > 0 do %>
  <h3>Domains</h3>
  <ul>
  <%= for domain <- @object.domains do %>
    <li><%= domain.name %></li>
  <% end %>  
  </ul>
<% end %>

<div>
  <%= link gettext("Index"), to: Routes.object_path(@conn, :index) %> | 
  <%= link gettext("Edit"), to: Routes.object_path(@conn, :edit, @object) %> | 
  <%= 
    link gettext("Delete"), 
      to: Routes.object_path(@conn, :delete, @object), 
      method: :delete, 
      data: [confirm: gettext("Are you sure?")]
  %>  
</div>