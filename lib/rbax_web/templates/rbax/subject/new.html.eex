<h1><%= gettext "New Subject" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.subject_path(@conn, :create)) %>

<span><%= link gettext("Cancel"), to: Routes.subject_path(@conn, :index) %></span>