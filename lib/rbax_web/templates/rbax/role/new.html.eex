<h1><%= gettext "New Role" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.role_path(@conn, :create)) %>

<span><%= link gettext("Cancel"), to: Routes.role_path(@conn, :index) %></span>