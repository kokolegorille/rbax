<h1><%= gettext "New Operation" %></h1>

<%= render "form.html", Map.put(assigns, :action, Routes.operation_path(@conn, :create)) %>

<span><%= link gettext("Cancel"), to: Routes.operation_path(@conn, :index) %></span>